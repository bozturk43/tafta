import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['firebasestorage.googleapis.com','tafta-pied.vercel.app'],
  },
  /* config options here */
};

export default nextConfig;
